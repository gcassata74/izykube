import { YamlDirective } from './yaml.directive';

describe('YamlDirective', () => {
  it('should create an instance', () => {
    const directive = new YamlDirective();
    expect(directive).toBeTruthy();
  });
});
